// src/roles/student/pages/MessagesPage.jsx
import React from 'react';
import { Box, Typography } from '@mui/material';

const MessagesPage = () => (
  <Box p={4}>
    <Typography variant="h4">Messages</Typography>
    <Typography>View and send messages to peers and instructors.</Typography>
  </Box>
);

export default MessagesPage;